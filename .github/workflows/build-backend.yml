name: Build & Push Backend

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  backend-ci:
    name: Lint & Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          # cache: "pnpm"

      - name: Install pnpm
        run: corepack enable && corepack prepare pnpm@latest --activate

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Debug TypeScript and ESLint versions
        run: |
          pnpm ls typescript @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint

      - name: Build shared workspace
        run: pnpm --filter @artify/shared build

      - name: Lint
        run: pnpm --filter backend lint

      - name: Type Check
        run: pnpm --filter backend type-check

      - name: Run Tests
        run: pnpm --filter backend test

  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Lint
        run: |
          pnpm install --frozen-lockfile
          pnpm --filter backend lint

      - name: Test
        run: |
          pnpm install --frozen-lockfile
          pnpm --filter backend test

      - name: Install pnpm
        run: npm install -g pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build backend Docker image
        run: |
          docker build -t artify-backend -f Dockerfile.backend .

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push backend Docker image to GitHub Container Registry
        run: |
          docker tag artify-backend ghcr.io/${{ github.repository_owner }}/artify-backend:latest
          docker push ghcr.io/${{ github.repository_owner }}/artify-backend:latest
